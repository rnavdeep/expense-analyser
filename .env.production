# Production build (npm run build) — API calls are relative so they route through
# the nginx /api proxy in the container (see nginx.conf -> ${BACKEND_API_URL}).
# Dev (npm run dev) keeps using .env.
VITE_APP_API_URL=/api
